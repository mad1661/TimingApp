// Single source of truth for the app version shown in the UI (navbar footer).
// Bump this on each release so upgrades are visible in the running app; keep it
// in sync with the "version" field in package.json.
export const APP_VERSION = "1.1.0";
